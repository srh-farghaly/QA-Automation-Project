version: 2.1

jobs:
  ui_tests:
    docker:
      - image: cimg/node:18.17.1
    working_directory: ~/project/QA-automation-Task
    steps:
      - checkout
      - run:
          name: Install Chromium and ChromeDriver
          command: |
            sudo apt-get update
            sudo apt-get install -y chromium-browser chromium-chromedriver
      - run:
          name: Verify ChromeDriver Path
          command: ls -l QA-automation-Task/node_modules/chromedriver/lib/chromedriver
      - run:
          name: Navigate to QA-automation-Task and install dependencies
          command: |
            cd QA-automation-Task
            npm install
      - run:
          name: Reinstall ChromeDriver
          command: |
            cd QA-automation-Task
            npm install chromedriver --save-dev
      - run:
          name: Verify node_modules/.bin exists
          command: ls -l QA-automation-Task/node_modules/.bin
      - run:
          name: Fix Permissions
          command: |
            chmod -R 755 QA-automation-Task/node_modules
      - run:
          name: Run Nightwatch Tests
          command: |
            cd QA-automation-Task
            npm run test

workflows:
  version: 2
  pipeline:
    jobs:
      - ui_tests
